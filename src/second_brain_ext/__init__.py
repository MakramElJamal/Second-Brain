"""second_brain_ext: token-light retrieval layer for obsidian-web-mcp.

Adds ranked, stingy `search_notes` / `get_note` tools on top of the upstream
secure server via its public extension seam (no fork of core required).
"""

__version__ = "0.1.0"
